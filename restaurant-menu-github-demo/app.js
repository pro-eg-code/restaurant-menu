const statusEl=document.getElementById('status');const menuEl=document.getElementById('menu');
async function loadMenu(){try{const r=await fetch(`./menu.json?v=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);const d=await r.json();const p=Array.isArray(d.products)?d.products:[];const visible=p.filter(x=>x.active!==false);menuEl.innerHTML=visible.map(x=>{const img=x.image||'https://placehold.co/800x600?text=No+Image';const price=Number(x.price||0).toLocaleString('en-US',{maximumFractionDigits:2});return `<article class="card"><img src="${escapeHtml(img)}" alt="${escapeHtml(x.name||'')}" loading="lazy"><div class="card-body"><div class="card-top"><div><h3>${escapeHtml(x.name||'Unnamed')}</h3><div class="category">${escapeHtml(x.category||'Menu')}</div></div><div class="price">${price} EGP</div></div><p class="description">${escapeHtml(x.description||'')}</p></div></article>`}).join('');statusEl.textContent=`${visible.length} product${visible.length===1?'':'s'}`}catch(e){console.error(e);statusEl.textContent='Could not load the menu.'}}
function escapeHtml(v){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}loadMenu();
